<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.g_pro_justlikemeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconn"
        android:label="Just Like Me App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GproJustLikeMeApp">
        <activity
            android:name=".AddPostActivity" />
        <activity android:name=".PostDetailActivity" />
        <activity android:name=".orgActivity" />
        <activity android:name=".UserTypeActivity" />
        <activity android:name=".Community_participantAdd_Activity" />
        <activity android:name=".AdminActivity" /> <!-- android:exported="false" /> -->
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".StartActivity" />
        <activity
            android:name=".LogInActivity"
            android:parentActivityName=".StartActivity" />
        <activity
            android:name=".MessageActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CommunityChatActivity"
            android:parentActivityName=".StartActivity" />
        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.FirebaseService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".notifications.FireBaseMesseging"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>